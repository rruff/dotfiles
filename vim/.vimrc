" Syntax highlighting
syntax on

" Show line numbers on the left
set number

" Show line and column number in status bar
set ruler

set autoindent

" Use 4 space indent instead of tabs
set shiftwidth=4
set softtabstop=4
set expandtab

" Make the font bigger because I'm old
set guifont=Menlo-Regular:h14
